<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ImageView
            android:scaleType="centerCrop"
            android:src="@drawable/lock_img"
            android:id="@+id/video_show_iv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
            <TextView
                android:id="@+id/video_show_tv"
                android:text="wChat(3)"
                android:maxLines="1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

    </LinearLayout>
    <TextView
        android:id="@+id/new_find"
        android:visibility="gone"
        android:layout_gravity="right"
        android:textColor="@android:color/holo_red_light"
        android:text="@string/new_find"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:ignore="RtlHardcoded" />
    <CheckBox
        android:button="@null"
        android:id="@+id/has_selected"
        android:visibility="gone"
        android:background="@drawable/my_checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        tools:ignore="RtlHardcoded" />
</FrameLayout>